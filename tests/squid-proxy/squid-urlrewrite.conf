# ArchLinux
rewrite ^.*/(archlinux/.*)$ http://mirrors.iu13.net/$1

# CentOS
rewrite ^.*/(centos/.*)$ http://mirrors.iu13.net/$1
rewrite ^.*/(centos-stream/.*)$ http://mirrors.iu13.net/$1

# EPEL
#rewrite ^.*/(epel/.*)$ http://mirror.xenyth.net/$1

# Fedora
rewrite ^.*/(fedora/linux/.*)$ http://mirror.xenyth.net/$1

# Rocky Linux
rewrite ^.*/(rocky/.*)$ http://mirrors.iu13.net/$1
